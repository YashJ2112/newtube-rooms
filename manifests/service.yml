apiVersion: v1
kind: Service
metadata:
    name: "newtube-64ea"
    labels:
        app: "newtube-64ea"
spec:
    type: ClusterIP
    ports:
    - port: 3001
      targetPort: 3001
      protocol: TCP
      name: http
    selector:
        app: "newtube-64ea"
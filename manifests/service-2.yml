apiVersion: v1
kind: Service
metadata:
    name: "newtubeaks-b98b"
    labels:
        app: "newtubeaks-b98b"
spec:
    type: LoadBalancer
    ports:
    - port: 3001
      targetPort: 3001
      protocol: TCP
      name: http
    selector:
        app: "newtubeaks-b98b"
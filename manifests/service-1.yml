apiVersion: v1
kind: Service
metadata:
    name: "newtube-2b37"
    labels:
        app: "newtube-2b37"
spec:
    type: ClusterIP
    ports:
    - port: 3001
      targetPort: 3001
      protocol: TCP
      name: http
    selector:
        app: "newtube-2b37"
version: '3'

services:
  rooms-service:
    container_name: rooms-service
    image: rooms-service:1.0.0
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - '3000:3000'
    env_file:
      - .env
